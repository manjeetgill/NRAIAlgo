{
  "name": "nraialgo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "workspaces": [
    "backend/nodejs",
    "frontend/nextjs",
    "shared/typescript"
  ],
  "scripts": {
    "prepare-contracts": "npm run build --workspace shared/typescript",
    "dev": "npm run prepare-contracts && npm run db:setup --workspace backend/nodejs && concurrently -n api,web -c blue,green \"npm run dev --workspace backend/nodejs\" \"npm run dev --workspace frontend/nextjs\"",
    "build": "npm run prepare-contracts && npm run build --workspaces",
    "test": "npm run prepare-contracts && node tests/run.mjs",
    "test:api": "npm run prepare-contracts && node tests/run.mjs api",
    "test:web": "npm run prepare-contracts && node tests/run.mjs web",
    "test:contracts": "npm run prepare-contracts && node tests/run.mjs contracts",
    "test:smoke": "node tests/run.mjs smoke",
    "lint": "npm run lint --workspaces && eslint --config tests/eslint.config.mjs tests",
    "typecheck": "npm run prepare-contracts && npm run typecheck --workspaces && tsc --project tests/tsconfig.json && tsc --project tests/web/tsconfig.json"
  },
  "overrides": {
    "kiteconnect": {
      "mocha": {
        "serialize-javascript": "7.0.5"
      }
    }
  },
  "devDependencies": {
    "adm-zip": "^0.6.1",
    "concurrently": "10.0.5",
    "typescript": "5.9.3"
  }
}
