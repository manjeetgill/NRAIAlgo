# No passwords, vault keys or broker tokens in this file.
APP_DOMAIN=algo.example.com
ACME_EMAIL=you@example.com
# Use the exact deployed commit, not "latest". Keep previous images for rollback.
RELEASE_TAG=replace-with-git-revision
